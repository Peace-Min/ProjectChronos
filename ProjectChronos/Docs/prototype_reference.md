# Canonical Prototype Reference

## Current canonical prototype

As of `2026-04-03`, the canonical prototype for the report timeline break symbol is:

- the image attached by the user in the Codex thread on `2026-04-03`
- the image that shows the break as two vertical S-curve strokes visually fused with the baseline
- the image where the baseline and S break read as one continuous axis system

## Important rule

Do **not** use this file as the canonical prototype:

- `C:\Users\CEO\source\repos\ProjectChronos\ProjectChronos\output\report_timeline_prototype.png`

That local PNG is an older, incorrect reference and must be treated as deprecated for visual acceptance.

## Interpretation rule

When the user says `prototype`, interpret it as:

- the `2026-04-03` user-attached image in the Codex thread
- not any earlier local export PNG

## Visual acceptance notes

The canonical prototype requires:

- the baseline and S break to look visually continuous
- the S break to read as part of the axis, not as an inserted icon
- the break to preserve the same visual language as the main line

## Replacement path

If the exact user-provided image binary is later supplied as a local file, store it at:

- `C:\Users\CEO\source\repos\ProjectChronos\ProjectChronos\Docs\prototype_canonical_20260403.png`

and replace this note to point directly at that file.
